schema_version: 1
workspaces:
  work:
    default_route: chat
    routes:
      chat:
        tiers:
          - targets:
              - id: claude-local
                model: claude-local
                protocol: messages
                connection:
                  custom:
                    base_url: http://127.0.0.1:18080/v1
